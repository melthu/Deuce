# Minimal deps for the scheduled data refresh + retrain + static export.
# No torch — the dataset module degrades gracefully without it, and only
# DeepFM needs it. No streamlit either: the export is a plain script.
numpy
pandas
scikit-learn
xgboost
lightgbm
catboost
requests
beautifulsoup4
# TreeSHAP attributions, precomputed into site/data by export_static.py
shap
